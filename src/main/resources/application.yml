spring:
  datasource:
    # H2 ?????? ??
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    # JPA ??
    hibernate:
      ddl-auto: update # ?????? ?? ? Entity ???? ??? ????
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # ???, ???? ??? ??
    properties:
      hibernate:
        format_sql: true # SQL ?? ??? ??? ?? ???
        highlight_sql: true # ??? ???? SQL ?????
        dialect: org.hibernate.dialect.PostgreSQLDialect # H2?? PostgreSQL ??? ????? ?? (TimescaleDB?? ???)
    show-sql: true # ??? SQL ?? ??

  h2:
    console:
      enabled: true # H2 ?? ???
      path: /h2-console # H2 ?? ?? ?? (?: http://localhost:8080/h2-console)

logging:
  level:
    org.hibernate.SQL: DEBUG # ???? ?? SQL ?? ??
    org.hibernate.orm.jdbc.bind: TRACE # SQL ??? ???? ??